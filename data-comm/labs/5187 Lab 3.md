Jaskin Kabir
801186717
![[Pasted image 20251201171423.png]]
1. What is the IP address and TCP port number used by the client computer (source) that is transferring the alice.txt file to gaia.cs.umass.edu? 
	1. **SRC IP: 192.168.50.66**
	2. **SRC Port: 59902**
2. What is the IP address of gaia.cs.umass.edu? On what port number is it sending and receiving TCP segments for this connection?
	1. **DST IP: 128.119.245.12**
	2. **DST Port: 80**
![[Pasted image 20251201172152.png]]
3. What is the sequence number of the TCP SYN segment that is used to initiate the TCP connection between the client computer and gaia.cs.umass.edu? What is it in this TCP segment that identifies the segment as a SYN segment?
	1. **Sequence Number (raw): 3555782354**
	2. **SYN segment because the SYN flag is set**
![[Pasted image 20251201172522.png]]
4. What is the sequence number of the SYNACK segment sent by gaia.cs.umass.edu to the client computer in reply to the SYN? What is it in the segment that identifies the segment as a SYNACK segment? What is the value of the Acknowledgement field in the SYNACK segment? How did gaia.cs.umass.edu determine that value?
	1. **Sequence Number (raw): 1149921213**
	2. **SYNACK segment because the SYN and ACK flags are set**
	3. **Acknowledgment number (raw): 3555782355**
	4. **This is the ack number of the SYN packet plus one. This acknowledges the SYN packet's sequence number**
![[Pasted image 20251201173146.png]]
5. What is the sequence number of the TCP segment containing the header of the HTTP POST command? How many bytes of data are contained in the payload (data) field of this TCP segment? Did all of the data in the transferred file alice.txt fit into this single segment?
	1. **Sequence Number (raw): 3555782355**
	2. **TCP payload (14600 bytes)**
	3. **alice.txt is 152KB, so the data did not fully fit into this segment.**
![[Pasted image 20251201175232.png]]
6. Consider the TCP segment containing the HTTP “POST” as the first segment in the data transfer part of the TCP connection
	1. At what time was the first segment (the one containing the HTTP POST) in the data-transfer part of the TCP connection sent? 
		1. **2.18115**
	2. At what time was the ACK for this first data-containing segment received?
		1. **2.238012**
	3. What is the round-trip-time (RTT) for this first data-containing segment?
		1. **56.862ms**
	4. What is the RTT value for the second data-carrying TCP segment and its ACK?
		1. **2.278556–2.238100=40.456ms**
	5. Plot the RTT of all the Reassembled TCP segments and show a screenshot.![[rtt-graph.png]]
7. What is the length (header plus payload) of each of the first four data-carrying TCP segments?
	1. **14620**
	2. **14620**
	3. **27760**
	4. **35060**
8. What is the minimum amount of available buffer space advertised to the client by gaia.cs.umass.edu among these first four data-carrying TCP segments3? Does the lack of receiver buffer space ever throttle the sender for these first four data- carrying segments?
	1. **6420 Bytes**
	2. **There is no throttling. The window size, and the amount of data sent by the sender, both increase over the course of the first 4 transmissions**
9. Are there any retransmitted segments in the trace file? What did you check for (in the trace) in order to answer this question?
	1. **I didn't see any retransmitted segments. I first looked for any time 3 duplicate acknowledgements were sent, which I did not find. Next, I looked for any two packets with the same field values, which I also could not find. I also used the `tcp.analysis.retransmission` filter in wireshark, which found nothing.**
10. How much data does the receiver typically acknowledge in an ACK among the first ten data-carrying segments sent from the client to gaia.cs.umass.edu? Can you identify cases where the receiver is ACKing every other received segment among these first ten data-carrying segments?
	1. I used excel to create this chart.
	2. ![[Pasted image 20251201185653.png]]
	3. It shows that for the majority of ACK segment sent by the server, 1460 bytes are acknowledged. However, at one point, the server ACKs 3 times this amount, right before the second segment is sent. After the second segment is sent, the server ACKs twice the 1460 byte value for a brief period before returning to 1460 per bytes per ACK. The final ack for this file transfer acknowledges 1027 bytes, which is likely the remainder of the file that could not fill the 1460 byte division.
11. What is the throughput (bytes transferred per unit time) for the TCP connection? Explain how you calculated this value
	1. **I decided to calculate the useful throughput, that is the size of the `alice.txt` file divided by the time between the SYN segment and the final ACK for the transmission**
	2. $$\frac{152\text{KB}}{2.370658s-2.140164s}=659.453 \frac{\text{KB}}{\text{second}}$$
	3. ![[Pasted image 20251201191703.png]]
	4. **The Wireshark throughput graph shows an average throughput of 1.226Mbps**
This is the full graph generated by wireshark. The first 2 and last 3 segments are not part of the `alice.txt` transmission, so I cropped the graph to only include the packets involved in this transmission.
![[timeseq.png]]
Cropped:
![[Pasted image 20251201194412.png]]
12. Comment on whether this looks as if TCP is in its slow start phase, congestion avoidance phase, or some other phase
	1. **TCP is in its slow start phase. The sequence number seems to double after each fleet, except for the final transmission. This is because the bulk of the data has already been sent, and not enough remains to double the transmission size**
13. These “fleets” of segments appear to have some periodicity. What can you say about the period?
	1. The time between fleets seems to be around 0.04-0.06 seconds. This is also the range of the RTTs measured earlier, which suggests that each fleet is sent as soon as the first segment of the previous fleet has been acknowledged.